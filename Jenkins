pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Run Payroll Job') {
            steps {
                sh 'java -cp target/hrms_project-1.0-SNAPSHOT.jar com.hrms.job.PayrollJob'
            }
        }
    }
}
